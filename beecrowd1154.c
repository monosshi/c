#include<stdio.h>
int main()
{
    int i=0,num;
    float b,count=0;
    while(1){
        scanf("%d", &num);
        if(num < 0)break;
        i = i + num;
        count;
    }
    b = i/count;
    printf("%.2f\n", b);
    return 0;
}
