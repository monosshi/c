#include<stdio.h>
int main(){
    int max,x,y;
    scanf("%d %d",&x,&y);
    max= (x>y)?printf("Maximum value is %d ",x):printf("Maximum value is %d ",y);
    return 0;
}
// SHORT HAND IF ELSE CONDITION